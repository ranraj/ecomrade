<%= render @purchaseorder %>

<div>
  <%= link_to t("purchaseorder.action.edit"), edit_purchaseorder_path(@purchaseorder) %> |
  <%= link_to  t("purchaseorder.action.back"), purchaseorders_path %>

  <%= button_to  t("purchaseorder.action.destroy"), @purchaseorder, method: :delete, class: 'btn btn-danger' %>
</div>
