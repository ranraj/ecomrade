<%= form_with(model: purchaseorder) do |form| %>
  <% if purchaseorder.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(purchaseorder.errors.count, "error") %> prohibited this purchaseorder from being saved:</h2>

      <ul>
        <% purchaseorder.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label t("purchaseorder.lable.status"), style: "display: block" %>
    <%= form.text_field :status, placeholder: t("purchaseorder.lable.status") %>
  </div>

  <div class="form-group "> 
    <%= form.number_field :user_id, id: :company_user_id, value: current_user.id, type: :hidden %>
  </div>
  
  <div>
    <%= form.submit t("purchaseorder.action.save"), class: 'btn btn-primary' %>
  </div>
<% end %>
