<h1><%= t("company.heading.edit")%></h1>

<%= render "form", company: @company %>

<br>

<div>
  <%= link_to t("company.action.show"), @company %> |
  <%= link_to t("company.action.back"), companies_path %>
</div>
